#!/bin/bash
# install-backup-timer.sh - DEPRECATED
# Use 'sudo btrdasd setup' instead, which reads config.toml
# and installs systemd units with the correct paths and schedules.

echo "DEPRECATED: Use 'sudo btrdasd setup' instead" >&2
exit 1
